spec:
  name: spring-boot-greeting
  services:
    dockerfile_path: Dockerfile
    github:
      branch: main
      deploy_on_push: true
      repo: tajacks/doap-spring-boot
    name: doap-spring-boot
    source_dir: /
